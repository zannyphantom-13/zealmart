# Kora Pay (test) - keep secret key private and set on server
# Client-side public key (exposed to browser)
VITE_KORA_PUBLIC_KEY=pk_test_xxx

# Server-side secret key (DO NOT COMMIT to repo)
KORA_SECRET_KEY=sk_test_xxx

# Example server endpoints (implement on your backend):
# POST /api/kora/create-payment -> creates payment and returns { checkoutUrl, reference }
# POST /api/kora/verify-payment -> verifies a payment by reference
